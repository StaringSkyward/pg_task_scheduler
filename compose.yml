services:
  db:
    image: postgres:13
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: pg_scheduler
    ports:
      - "55432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/13/pg_scheduler
volumes:
  postgres-data:
